const TonopahController=require("../../../src/server/controllers/TonopahController");
const TableController=require("../../../src/server/controllers/TableController");

describe("TableController",()=>{
	it("can start a game",async ()=>{
/*		let controller=new TonopahController();
		let tableState=await controller.load(123);

		controller.message(tableState,"olle",{
			action: "seatJoin",
			seatIndex: 4
		});

		controller.message(tableState,"kalle",{
			action: "seatJoin",
			seatIndex: 5
		});

		expect(tableState.dealerSeatIndex).toEqual(4);*/
/*		controller.message(tableState,"olle",{
			action: "postBB"
		});

		controller.message(tableState,"kalle",{
			action: "postBB"
		});*/
	});
});