{
  "private": true,
  "name": "tonopah",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/client/tonopahclient.jsx --jsx-factory=h --jsx-fragment=Fragment --bundle --outfile=res/bundle/bundle.js --inject:src/utils/preact-shim.js --define:GOMAXPROCS=1",
    "test": "jasmine"
  },
  "devDependencies": {
    "esbuild": "^0.8.46",
    "jasmine": "^3.6.4"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "react-spring": "^8.0.27"
  }
}
